const { FormaPagamento, SituacaoReserva, MotivoCancelamento, StatusPagamento } = require('../enums');

module.exports = {
  FormaPagamento,
  SituacaoReserva,
  MotivoCancelamento,
  StatusPagamento
};