<!DOCTYPE html>
<html lang="en">
<head>
    <%-include('../partials/header.ejs')%>
    <script src="/js/validaInputDataPeriodo.js" ></script>
</head>
<body  >
    
    <%-include('../partials/navbar.ejs')%>

    <div class="conteudo" >

            <!-- Menssagem do topo da página em jumbotron -->
            <div class="jumbotron jumbotron-fluid">
                <h1 class="display-4" >Alterar data da reserva.</h1>
                <!-- <p class="lead">Clientes cadastrados podem ver seu histórico de locações, gerenciar suas reservas e receber -->
                    <!-- descontos.</p> -->
            </div>


        <div class="container" >
            <!-- <h3>Reboque escolhido</h3> -->
            <div class="lista-reboques">
                
                <div class="card card-reserva" style="width: 25rem;">
                    <img src="../../<%=reserva.reboque.foto%>" class="card-img-top" alt="...">
                    <div class="card-body">
                      <h5 class="card-title"> Tipo: <%=reserva.reboque.modelo%></h5>
                      <h5 class="card-text">Diária R$: <%=reserva.reboque.valorDiaria%>,00</h5>
                      <hr>
                      <h5 class="card-text"><%=reserva.reboque.descricao%></h5>
                    </div>
                </div>


                <div class="card card-reserva" style="width: 25rem;">
                    <div class="card-body">
                        <form class="needs-validation" novalidate action="/cliente/editarDataReserva" method="POST">
                            <h5>Alterar Período</h5>
                            <hr>
                            
                            <div class="input-group mb-3">
                                <span class="input-group-text" >Data de Retirada</span>
                                <input class="form-control" id="dataInicio" type="date" name="dataInicioNova" required>
                                <div class="valid-feedback">
                                    <!-- Looks good! -->
                                </div>
                                <div class="invalid-feedback">
                                    Verifique a data.
                                </div>
                            
                            </div>

                            <div class="input-group mb-3">
                                <span class="input-group-text" >Data de Entrega</span>
                                <input class="form-control" id="dataFim" type="date" name="dataFimNova" required>
                                <div class="valid-feedback">
                                    <!-- Looks good! -->
                                </div>
                                <div class="invalid-feedback">
                                    Verifique a data.
                                </div>
                            </div><br>                        
                            

                            <input type="hidden" name="diarias" value="<%=reserva.diarias%>" >
                            <input type="hidden" name="dataInicioAntiga" value="<%=reserva.dataSaida%>" >
                            <input type="hidden" name="reboquePlaca" value="<%=reboque.placa%>" >
                            <input type="hidden" name="idReserva" value="<%=reserva.id%>" >
                            <br><input class="btn btn-primary" id="botao" type="submit" value="Finalizar">
                            
                            
                            <% if(mensagem){%>
                                <div class="alert alert-danger">
                                    <strong>Atenção!</strong>
                                    <%= mensagem %>
                                </div>
                            <% } %>

                        </form>
                    </div>
                </div>

                <divc class="card card-reserva" style="width: 25rem;">
                    <div class="card card-body">
                        <% if(reservas != '') {%>
                            <div class="alert alert-info">
                                <h5 class="card-text">Indisponível para estas datas</h5>
                                <% reservas.forEach(function(reserva) { %>
                                    <p>
                                        de <%=reserva.dataSaida.toString().slice(8,10)+'/'+reserva.dataSaida.toString().slice(5,7)+'/'+reserva.dataSaida.toString().slice(0,4)%> 
                                        à  <%=reserva.dataChegada.toString().slice(8,10)+'/'+reserva.dataChegada.toString().slice(5,7)+'/'+reserva.dataChegada.toString().slice(0,4)%>
                                    </p>
    
                                <%})%>
                            </div>
                        <% } %>  
                    </div>
                </divc>

            </div>
        </div>
    </div>

    <%-include('../partials/footer.ejs')%>
</body>
</html>