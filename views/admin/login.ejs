<!DOCTYPE html>
<html>
<head>
    <%-include('../partials/header'); %>
</head>
<body>
    <div class="conteudo" >
        <div class="container">
            <div class="card" >
                <div class="card-body">
                    <h3 class="card-title">Autenticação do Administrador</h3>
                    <form action="/admin/login/salvar" method="POST">
                        <div class="form-group">
                            <label for="login">Login</label>
                            <input class="form-control" id="email" type="text" name="email" required>
                        </div>
                        <div class="form-group">
                            <label for="senha">Senha</label>
                            <input class="form-control" id="senha" type="password" name="senha" required>
                        </div>
                        <br>
                        <a class="btn btn-primary" href="/admin/painel">Voltar</a>
                        <input class="btn btn-success" type="submit" value="Login">
                        <a href="/admin/cadastro/" class="btn btn-primary">Novo admin.</a>
                    </form>
                </div>
            </div>
            <% if(mensagem){ %>
            <div class="alert alert-danger">
                <strong>Alerta!</strong> <%= mensagem %>
            </div>
            <% } %>
        </div>
    </div>

</body>
</html>